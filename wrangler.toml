name = "universal-read-api"
main = "src/index.ts"
compatibility_date = "2024-04-05"
compatibility_flags = ["nodejs_compat"]

[vars]
ENVIRONMENT = "production"

# Browser Rendering binding (optional - requires paid plan)
# Uncomment below if you upgrade to Workers Paid and want browser rendering
# [browser]
# binding = "BROWSER"
